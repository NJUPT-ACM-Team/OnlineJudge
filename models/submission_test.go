package models

/*
import (
	"testing"
	"time"
)

// need to be tested
func TestSubmissionModel(t *testing.T) {
	subm := NewSubmissionModel()
	sub := Submission{
		Status:          "Accepted",
		StatusCode:      "ac",
		TestCasesPassed: 10,
		SubmitTime:      time.Now(),
		IsContest:       false,
		MetaPidFK:       10,
		UserIdFK:        20,
	}
	if _, err := subm.Insert(&sub); err != nil {
		t.Errorf("Failed to insert, %s", err)
	}
}
*/
